Requirements:
    
    scikit-learn
    numpy
    seqfold (see tRpredictor_func header)

All the models was trained with scikit learn version 0.24.1. 
The program might give warnings if you use a later version. 
